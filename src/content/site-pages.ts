// Every page the site has: the portfolio and the CV. Read by the speculation
// rules (scripts/page-scripts.ts) and by the fallback that prefetches where
// the rules are not understood (layout/page.ts).
export const sitePages: readonly string[] = ['/', '/cv.html']
